import { experience } from '../data/experience';
export function Journey() { return <section className="content-section section-frame" id="journey"><div className="section-label">01 / Journey</div><div className="section-content"><h2>A path through products, research, and the people around them.</h2><div className="timeline">{experience.map((item) => <article className="timeline-item" key={`${item.role}-${item.organization}`}><span className="mono">{item.period}</span><div><h3>{item.role}</h3><p className="meta">{item.organization} · {item.location}</p><p>{item.summary}</p></div></article>)}</div></div></section>; }